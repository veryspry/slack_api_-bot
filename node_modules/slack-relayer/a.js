
const createSlacker = require('./');

process.env.SERVICE_NAME = 'slack-relayer-service-name'


 // returns the object just in case
const relayer = createSlacker({
	channel: 'ambient-test',
	token: 'xoxb-63047932256-VtHXPx8PZfmU1XsMfjI2juv1',
	event: 'slack',
	prefix: '['+ (process.env.SERVICE_NAME?' '+process.env.SERVICE_NAME:'') + '@' + require('os').hostname() + '] '
});

setInterval(()=>process.emit('slack','hello','omg hello'),100)

